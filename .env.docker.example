# Root-level Docker environment template
# Copy to .env when using docker compose in production mode.

# URL allowed by backend CORS for frontend app
FRONTEND_URL=http://localhost:8080

# API URL baked into frontend production build
VITE_API_URL=http://localhost:3000/api/v1
